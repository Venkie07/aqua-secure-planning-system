 <div className="card m-2 sm:m-3 md:m-4 flex justify-between items-center px-3 sm:px-4 md:px-6 py-3 sm:py-4">
        <div className="flex items-center gap-2 sm:gap-3 md:gap-4">
          <button
            onClick={onMenuClick}
            className="text-2xl text-slate-600 hover:text-slate-900 transition-colors md:hidden"
            aria-label="Menu"
          >
            ☰
          </button>
          <div className="flex items-center gap-0.5 sm:gap-1 md:gap-2">
            <button
              onClick={() => navigate('/')}
              className="px-2 sm:px-3 py-1.5 text-sm sm:text-base font-semibold text-slate-600 hover:text-slate-900 transition-colors rounded-lg hover:bg-slate-100"
              title="Home"
            >
              ←
            </button>
            <h1 className="text-lg sm:text-xl md:text-2xl font-bold hidden sm:block bg-gradient-to-r from-cyan-600 to-blue-700 bg-clip-text text-transparent">
              ASPS
            </h1>
          </div>
        </div>

        <div className="flex items-center gap-2 sm:gap-3 md:gap-4">
          <span className="text-slate-600 text-xs sm:text-sm hidden sm:block">{user?.name}</span>
          <button
            type="button"
            className="w-9 h-9 sm:w-10 sm:h-10 rounded-full bg-gradient-to-r from-cyan-600 to-blue-700 flex items-center justify-center text-white text-sm sm:text-base font-bold hover:scale-110 transition-transform shadow-md"
            title={user?.company || 'Profile'}
          >
            {user?.name?.charAt(0).toUpperCase() || 'U'}
          </button>
        </div>
      </div>